id: https://neverblink.eu/linkml/tests/
description: |
  Model catalogue metadata model
name: test_meta

imports:
  - linkml:types

classes:
  Metadata:
    class_uri: https://neverblink.eu/linkml/tests/Metadata
    attributes:
      derived_equals:
        range: string
        description: This model's derived tree_root class should be equivalent to the value of this

      opt_in:
        range: Boolean
        description: Whether this schema should be omitted in the 'all' collection of the model catalogue
